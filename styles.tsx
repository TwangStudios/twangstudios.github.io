import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  content: {
        top: '50%',
        left: '50%',
        right: 'auto',
        bottom: 'auto',
        marginRight: '-50%',
        flexDirection: 'column',
    },
})